#pragma once

void drawFooter(
    const char* leftText = nullptr,
    const char* rightText = nullptr
);

void redrawFooter(
    const char* leftText = nullptr,
    const char* rightText = nullptr
);
